# Pillow pinned exact: locks the MEDIANCUT quantizer + LANCZOS resampler
# behaviour used by render_page1_preview() (#67). Patch-version drift can
# perturb output bytes; the committed PNG is the canonical artifact.
Pillow==10.3.0
opencv-python>=4.9
pikepdf>=8.0
# PDF page rasterization for resume page-1 hover thumbnail (#67).
# Pre-built wheels for win/mac/linux; no system poppler dep.
# Pinned for byte-stable PNG output across regenerations.
pymupdf==1.24.10
# M3: render-portfolio.py template engine. Wide range pin — Jinja2 3.x API
# (Environment, FileSystemLoader, autoescape, tojson filter) is stable
# enough that we don't need an exact pin. Lower bound 3.0 ensures tojson
# filter; upper bound <4.0 guards against future breaking changes.
jinja2>=3.0,<4.0
